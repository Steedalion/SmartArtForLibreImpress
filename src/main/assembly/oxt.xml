<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3
                              http://maven.apache.org/xsd/assembly-1.1.3.xsd">
    <id>oxt</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <!-- description.xml contains ${project.version}; read it from the
             filtered build output so Maven has substituted the variable. -->
        <fileSet>
            <directory>${project.build.outputDirectory}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>description.xml</include>
            </includes>
        </fileSet>
        <!-- All other OXT resources come straight from source — they do not
             need Maven filtering and .xcu files are not copied to target/classes/
             by the resource plugin (which only handles *.xml / *.properties). -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>META-INF/**</include>
                <include>Addons.xcu</include>
                <include>ProtocolHandler.xcu</include>
                <include>uno/**</include>
                <include>images/**</include>
            </includes>
        </fileSet>
        <fileSet>
            <!-- The jar must sit beside uno/SmartArtImpl.xml: the descriptor's
                 uri="smartart.jar" is resolved relative to the descriptor's own
                 location, so the jar has to live in uno/ too. -->
            <directory>${project.build.directory}</directory>
            <outputDirectory>/uno</outputDirectory>
            <includes>
                <include>smartart.jar</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>
